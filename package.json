{
  "private": true,
  "scripts": {
    "dev": "dotenv -- turbo run dev",
    "build": "dotenv -- turbo run build",
    "db:migrate": "dotenv -- turbo run db:migrate --experimental-ui"
  },
  "dependencies": {
    "dotenv-cli": "^7.4.2",
    "turbo": "^1.13.3"
  }
}
